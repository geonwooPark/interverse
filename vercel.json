{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "buildCommand": "rm -rf server && npm install && cd types && npm install && cd ../client && tsc && vite build",
  "public": "./client"
}
