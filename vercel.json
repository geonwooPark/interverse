{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "buildCommand": "rm -rf server && npm install && cd types && npm install && cd ../client && npm run build",
  "devCommand": "cd client && npm run dev"
}
